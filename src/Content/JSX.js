import React from "react";

export default function JSX() {
  return (
    <div>
      <div className="paragraph-Heading">
        <h1>JSX</h1>
      </div>
      <div className="paragraph">
        <li>
          <b>JavaScript XML: </b> JSX is basically a syntax extension of
          JavaScript. It helps us to write HTML in JavaScript and forms the
          basis of React Development.
        </li>
        <li>
          JSX creates an element in React that gets rendered in the UI. It is
          transformed into JavaScript functions by the compiler at runtime.
        </li>
        <li>
          Error handling and warnings become easier to handle when using JSX  
        </li>
        <li>Ex: </li>
        
      </div>
    </div>
  );
}